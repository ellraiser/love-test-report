name: continuous-integration
on: [push, pull_request]

jobs:
  macOS:
    runs-on: macos-latest
    steps:
    - name: Love Test Report
      uses: ellraiser/love-test-report@latest
      with:
        name: Test Output
        path: output/*.xml
